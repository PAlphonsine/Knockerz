using UnityEngine;
using System.Collections;
using System.IO;
using System.Collections.Generic;

public class Test : MonoBehaviour {

	//private string XMLData = "hello"; //Have all of the file data in this string.

	// Use this for initialization
	void Start () {
		/*print(Application.persistentDataPath);
		//if (!System.IO.File.Exists(Application.persistentDataPath + "/filename.xml"))
			//System.IO.File.WriteAllText(Application.persistentDataPath + "/Hello.xml", XMLData);
		test2 _test2 = new test2();
		_test2.Save(Path.Combine(Application.persistentDataPath, "monsters.xml"));
		print (System.IO.File.ReadAllText (Application.persistentDataPath + "/Hello.xml"));*/
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
